/**
 * 导出默认主题配置
 */
const sysTheme = {
  // 'siderbar' 左侧菜单 | 'mix' 左侧+顶部菜单 | 'top' 顶部菜单 | 'mixbar' 双列菜单
  layout: 'siderbar',
  // 固定头部
  fixHeader: true,
  // 暗黑模式
  darkMode: false,
  // 主题颜色
  primaryColor: '#409eff',
  // 菜单宽度
  menuWidth: '266px',
  // 显示tab page
  tabPage: true,
  // 切换动画
  transitionName: 'Fade',
  // 背景图片
  backgroundImg: ''
}

export default sysTheme
